import 'package:flutter/material.dart';

class SEOScreen extends StatelessWidget {
  const SEOScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
     return Container(
      child: const Center(
        child: Text(
          'SEO Screen',
          style: TextStyle(fontSize: 50),
        ),
      ),
    );
  }
}

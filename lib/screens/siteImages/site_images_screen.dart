import 'package:flutter/material.dart';

class SiteImagesScreen extends StatelessWidget {
  const SiteImagesScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text(
          'Site Images Screen',
          style: TextStyle(fontSize: 50),
        ),
      ),
    );
  }
}
